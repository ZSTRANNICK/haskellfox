module EventHandler
    ( 
        --handleInput
    ) where

import Graphics.Gloss.Interface.Pure.Game
import Lib

--handleInput :: Event -> Scene -> Scene
--handleInput (EventKey (Char 'a') Down _ _) scene = 
